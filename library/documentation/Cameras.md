# Cameras

A camera lives in a scene and has a position, up and right vectors. It generates a new view and projection (perspective
/ orthogonal) transforms when the corresponding properties are modified.